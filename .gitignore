node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# Leftover from the local JSON history store this app used before Supabase; no code
# reads or writes here anymore, kept ignored so a stale local copy is never committed.
data/

# OCR language models, downloaded on first use (~5 MB each)
.tesseract-cache/
*.traineddata

# Local working prompt, not part of the project
prompt.xml

.env*
!.env.example

.DS_Store
Thumbs.db
